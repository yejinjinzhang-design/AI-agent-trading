#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: Read the problem statement in statement.txt
    // This is an interactive problem - communicate with the judge via stdin/stdout
    // Remember to flush output: cout.flush() or use endl

    return 0;
}
